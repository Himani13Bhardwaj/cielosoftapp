class AddBookModel {
  String? chapterName;
  String? chapterNumber;
  String? chapterURL;
  String? selectedFilePath;
  String? selectedFileName;

  AddBookModel(this.chapterName, this.chapterNumber, this.chapterURL,
      this.selectedFileName, this.selectedFilePath);
}
